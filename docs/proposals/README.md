# 可选方案

本目录沉淀**未采用 / 待评估**的设计方案。每篇文档形态相对自由，但建议至少包含：

- 提案的动机（解决了什么现有问题）
- 与当前实现的对比（diff 而非整段重写）
- 优劣评价：好处、坏处、拐点（什么时候这条方案的成本会反转）
- 决议：暂缓 / 采用 / 替代方案。决议明确后把文档移到 `docs/internal/` 或 `docs/inbound/` / `docs/outbound/` 对应位置，并在 PR 里说明转正路径。

未采用的方案保留在此目录，是为了避免相同讨论反复发生——下次有人提出"我们要不要用 X"时能直接翻到这篇看上次的结论。
